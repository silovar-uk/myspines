{
  "name": "myspines",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "node --check core.js && node --check commands.js && node --check ui.js && node --check editor.js && node --check library.js && node --check transfer.js && node --check bootstrap.js && node tests/smoke.mjs",
    "test": "node tests/smoke.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
